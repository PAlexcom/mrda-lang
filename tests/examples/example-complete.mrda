// Basic Declaration
//int c = 5 + 6;
//int x = 5 + 6, y = 5 * 4, z = 5 / 4;

// Compound Declaration
// char[4] s = ['a', 'b', 'c', 'd'];
bool a = True;

//int proc(val int c, name bool b) {
//    return (c);
//}

// Function Declaration
int func(val int a, res bool b, name char d, const float n, ref bool l) {
    // Basic Declaration
    int counter = a;
    //int z = proc(counter, b);
    //counter = a;

    // Iteration Statement
    while (b == True) {
        // Function Call
        int z = proc(counter, b);
        proc(counter, b);
    }

    // Selection Statement
    if (proc(counter, b) >= 8) {
        int counter = 2;
    } else {
        int counter = 4;
    }

    // Left Expression Declaration
    ++counter;
    --counter;

    // Jump Statement
    return (1+1);
}

void main() {
    int z = func(1, True, 'a', 3, False);
    return;
}

// Program execution
void run = main();